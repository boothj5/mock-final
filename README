Removing final keyword from bytecode, to enable mocking final classes

mock-final.sh will:

clean everything
compile the source
run a class to manipulate the bytecode of a final class
create a new class file with a different name for the manipulated bytecode
run a test mocking that new class

To do:

Incorporate fudging into classloading
